# 更新日志

## 2026-09-25 · 未发布：原生收藏分类与阅读体验改进

### 新增与改进

- **收藏时选择分类**：点击 Peek 浮窗内的收藏按钮，可选择当前账号的 X 原生收藏夹，也可仅保存到所有书签；已收藏的帖子可以再次选择分类，取消收藏有独立入口。
- **收藏夹与 X 联动**：读取原生文件夹并支持分页；新建和管理文件夹跳转 X 原生页面。无相应账号权限或读取失败时保留普通收藏，不建立本地替代分类。
- **阅读与回复增强**：整合贡献者 @dashaloveu 的 PR #3，包括主题自适应和手动切换、特殊字符显示、投票展示、展开子回复，以及回复时添加图片或视频。

### 本次验收修复

- **保留正文中的 @ 提及**：仅按 X 明确给出的可见文字范围隐藏回复收件人，不再猜测并删除用户主动输入的提及。
- **子回复继续加载**：每条分支分别维护分页、加载和重试状态；支持继续展开，限制视觉缩进而非读取层级，并防止循环引用和旧请求覆盖新状态。
- **混合内容粘贴**：粘贴图片和文字时尊重当前文字选区，保留选区外的草稿。
- **媒体回复失败处理**：上传和处理错误会中止发布；增加超时保护。发布结果不明确时保留草稿并阻止当前输入框重复提交。
- **减少浮窗空白**：扩展上下文失效时安全处理 HLS Worker 资源地址；渲染异常显示刷新入口。更新扩展后仍需刷新已打开的 X 页面。
- **日期避免重复翻译**：时间元素标记为不参与翻译，减少日期被翻译插件重复处理的问题。
- **收藏状态更准确**：文件夹列表去重，防止连续点击重复保存；收到确认结果后才更新收藏状态，失败时保留原状态。

### 验证与范围

- 构建成功，119 项自动测试全部通过，其中收藏夹专项 12 项。
- Chrome 中已实际打开分类面板并读取登录账号的 X 原生收藏夹，用户确认验收通过。
- 代理未独立核对真实收藏写入结果；媒体发布异常使用模拟响应验证，未为测试发布帖子。
- 本次不增加扩展权限、不持久化登录凭据。正式产品名保持 **Peek**；“收藏分类测试版 0925”仅用于本机测试包识别。

## 2026-09-16 · 未发布

- **品牌名称更新为 Peek**：扩展名称、工具栏提示、设置弹窗、浮层阅读器和演示页面统一使用新名称。
- **头像外部背景透明化**：移除头像轮廓外侧的洋红色背景，保留兔子、窗口边框及窗口内的洋红色。

## 2026-09-16 · v0.8.10

- 修复关注成功后仍报错：不再把响应缺少关系字段判定为写入失败。必要时通过 X 动态发现的 `UserByRestId` 查询核对状态，核对请求限制为 4 秒。
- 状态暂未同步时显示中性提示和“查看状态”入口，保留原有关系与粉丝数；点击入口前往 X 个人资料页，不会重复提交关注。
- 真正的 HTTP/API 错误仍按失败处理。新增写入成功但响应不完整、只读核对失败、错误账号和取消关注等回归用例。

## 2026-09-16 · v0.8.9

- 补齐 X 关注接口返回字符串 `id` 的兼容，继续支持旧 `id_str`，修复实际已关注却提示“X 未确认关注状态”的误判。
- 用户 ID 全程保持字符串，拒绝可能丢失精度的数字 ID；新增关注/取消关注两种响应格式的回归测试。

## 2026-09-16 · v0.8.8

- 修复资料卡点击“关注”提示 `CreateFriendship` 操作未加载的问题；关注和取消关注改为 X 网页实际使用的会话 REST 接口。
- 以服务器返回的关系和粉丝数更新浮层内同一作者；请求失败保留原状态并恢复按钮，私密账号待批准显示“已请求”。
- 增加关注桥接与按钮行为回归测试。更新后需要重新加载扩展并刷新 X 页面。

## 2026-08-18 · 今日更新

今天这轮没有再堆新入口，主要是把浮层里那些“已经能用，但还不够顺手”的地方继续磨细。目标很简单：让它更像 X 自己的操作习惯，也让正文和评论都更好读。

### 主要更新

- **界面更干净了**：标题统一为“兔崽插件”，去掉原帖和评论的说明文字、独立滚动提示、底部状态栏，把更多空间留给帖子和评论。
- **正文更好读了**：普通帖子和 X 长文都改成接近 X 列表页的字号；桌面端正文大约占 52%，评论区大约占 48%，正文略宽一些，但评论区仍然够用。
- **回复框更像 X 了**：没有输入时只显示一条比较窄的输入框；点击后再展开，输入的文字越多，输入框也会跟着变高。目前先只支持纯文字回复。
- **评论改成自动加载了**：滚动到评论区底部时会自动加载下一批，不再需要点击“加载更多评论”；真的没有更多内容以后，也不会继续请求或反复显示“正在加载更多评论”。
- **资料卡补齐了**：鼠标放在头像、昵称或账号名上，会显示接近 X 原生样式的资料卡；可以查看简介、关注数和粉丝数，也可以直接关注或取消关注。
- **资料卡不再闪了**：鼠标在头像、昵称和资料卡之间移动时，会复用同一张资料卡并留出关闭缓冲，不会再一闪一闪。
- **互动按钮更顺手了**：回复、转发、喜欢、收藏和分享继续保持 X 的顺序；鼠标移上去时，底色会把图标和数字一起包住，不再只亮一个过小的圆圈。
- **长文排版收紧了**：文章正文、标题和段落间距都缩小了一些，阅读密度和普通帖子更一致，不会在左栏显得特别大。
- **细节位置重新整理了**：评论排序和回复数量放在左边，“查看引用”固定在最右边；标题栏也变矮了，同一屏可以看到更多内容。

### 稳定性修复

- 评论分页碰到重复游标、没有新评论或没有下一页时，会马上停止加载。
- 在资料卡里关注或取消关注后，当前浮层中同一个作者的状态会一起更新。
- 所有 X 数据和互动仍然直接使用当前登录会话，不经过第三方服务器。
- 自动化测试增加到 **22 项，全部通过**。

### 对外发布摘要

今天又把「兔崽插件」打磨了一轮。
这次主要解决了几个每天都会碰到的小别扭：浮层更简洁，正文比评论区稍宽；回复框会像 X 一样按需展开；评论滚到底自动加载，真的到底以后也不会继续转圈；鼠标放到头像和昵称上能看到完整资料卡，而且不会闪。
互动按钮也重新调了一遍，鼠标移上去时，图标和数字会一起被包住。普通帖子和 X 长文的字号现在也更统一，阅读密度舒服了不少。

项目地址：https://github.com/xuntianx/tuzai-x-popover

## 2026-08-13 · v0.8.4

今天继续完善「兔崽 · X 帖子浮层阅读器」。这次没有增加新的页面，而是集中补齐真实使用中最影响阅读连续性的几个问题。

### 主要更新

- **帖子详情页不再重复打开浮层**：已经位于 `/status/...` 详情页时，保留 X 原生交互，只在主页、通知等帖子列表中启用浮层。
- **从通知打开回复时补齐完整上文**：左侧会展示从会话起点到当前回复的上下文，并自动定位到被点击的回复；向上滚动仍可查看前文。
- **恢复网站富卡片**：GitHub 等网站链接会显示 X 返回的标题、图片和来源信息，不再只剩下一条缩短链接。
- **X 长文直接加载完整正文**：插件会继续读取 Article 的正文数据，在左栏渲染标题、段落、标题层级、加粗、列表、引用和文章图片，而不是停留在封面与摘要卡片。
- **新回复立即出现在评论顶部**：纯文字回复发布成功后，会固定显示在当前评论列表最上方，并自动把右栏滚动到顶部，方便马上确认结果。

### 实现与稳定性

- 继续使用当前登录的 X 会话读取数据和执行互动，不经过第三方服务器。
- 动态读取 X 当前页面中的 GraphQL 操作定义，不硬编码查询 ID。
- 保留原帖与评论双栏独立滚动，不移动或复制 X 的 React DOM。
- 扩展版本升级到 `0.8.4`。
- 自动化测试增加到 **21 项，全部通过**。

### 对外发布摘要

今天继续更新了「兔崽 · X 帖子浮层阅读器」。
现在从通知点开一条回复，会带上它前面的完整对话并自动定位到当前回复；GitHub 等链接恢复了富卡片；X 长文也不再只是封面和摘要，而是可以直接在左栏阅读完整正文。
另外，发布回复后，新内容会立即出现在评论区顶部，不用再滚到列表最下面寻找。

项目地址：https://github.com/xuntianx/tuzai-x-popover
