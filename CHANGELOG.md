# 更新日志

## 2026-08-13 · v0.8.4

今天继续完善「兔仔 · X 帖子浮层阅读器」。这次没有增加新的页面，而是集中补齐真实使用中最影响阅读连续性的几个问题。

### 主要更新

- **帖子详情页不再重复打开浮层**：已经位于 `/status/...` 详情页时，保留 X 原生交互，只在主页、通知等帖子列表中启用浮层。
- **从通知打开回复时补齐完整上文**：左侧会展示从会话起点到当前回复的上下文，并自动定位到被点击的回复；向上滚动仍可查看前文。
- **恢复网站富卡片**：GitHub 等网站链接会显示 X 返回的标题、图片和来源信息，不再只剩下一条缩短链接。
- **X 长文直接加载完整正文**：插件会继续读取 Article 的正文数据，在左栏渲染标题、段落、标题层级、加粗、列表、引用和文章图片，而不是停留在封面与摘要卡片。
- **新回复立即出现在评论顶部**：纯文字回复发布成功后，会固定显示在当前评论列表最上方，并自动把右栏滚动到顶部，方便马上确认结果。

### 实现与稳定性

- 继续使用当前登录的 X 会话读取数据和执行互动，不经过第三方服务器。
- 动态读取 X 当前页面中的 GraphQL 操作定义，不硬编码查询 ID。
- 保留原帖与评论双栏独立滚动，不移动或复制 X 的 React DOM。
- 扩展版本升级到 `0.8.4`。
- 自动化测试增加到 **21 项，全部通过**。

### 对外发布摘要

今天继续更新了「兔仔 · X 帖子浮层阅读器」。
现在从通知点开一条回复，会带上它前面的完整对话并自动定位到当前回复；GitHub 等链接恢复了富卡片；X 长文也不再只是封面和摘要，而是可以直接在左栏阅读完整正文。
另外，发布回复后，新内容会立即出现在评论区顶部，不用再滚到列表最下面寻找。

项目地址：https://github.com/xuntianx/tuzai-x-popover
